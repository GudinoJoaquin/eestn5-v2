import {Noto_Sans, Archivo_Black, Montserrat, Roboto, Lato } from "next/font/google"

export const montserrat = Montserrat({subsets: ['latin']})

export const lato = Lato({subsets: ['latin'],  weight: ['900']})

export const roboto = Roboto({subsets: ['latin'], weight: ['900']})

export const notoSans = Noto_Sans({subsets: ['latin']})

export const archivoBlack = Archivo_Black({weight: '400', subsets: ['latin']})
